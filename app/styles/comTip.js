import { colors, otherStyles, baseStyles, fontStyles } from './common'

export const comTipView = {
    height: 25,
    justifyContent: 'center'
}

export const comTip = {
    ...fontStyles.normal,
    ...fontStyles.tinySize,
    color: '#FF2100'
}